node_modules/
build/
dist/
coverage/
.DS_Store
*.log

# Environment variables — NEVER commit secrets
.env*
!.env.example

# Next.js build output
.next/

# Uploaded media files (use Vercel Blob / S3 in production)
public/media/

# Payload generated types (can be regenerated)
# payload-types.ts  # keep this committed so Vercel gets the types

# Misc
atlas-credentials.env
